language: java

jdk:
  - oraclejdk8

os:
  - linux

branches:
  only:
    - master

cache:
  directories:
    - $HOME/.gradle

jobs:
  include:
  - stage: test
    script: ./gradlew check sonarqube -Dsonar.login=$SONAR_TOKEN --info

  - stage: publish
    if: branch = master
    script: ./gradlew publish
